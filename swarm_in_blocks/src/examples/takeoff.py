from swarm_in_blocks.swarm import Swarm

swarm = Swarm(swarm_name='goooCLoversss')

# Starts simulation
swarm.startSimulation()

# take off all clovers
swarm.takeOffAll()

# land all drones
swarm.landAll()
